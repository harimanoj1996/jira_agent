[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "life-os-agent"
version = "0.1.0"
description = "Bounded human-in-the-loop Jira decision-support agent"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
